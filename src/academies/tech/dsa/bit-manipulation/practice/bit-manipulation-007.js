import topic from '../../../_legacy/banks/dsa/bit-manipulation.js';

const problem = {
  ...topic.questions.find((question) => question.id === 'bit-manipulation-007'),
  category: 'dsa'
};

export default problem;
