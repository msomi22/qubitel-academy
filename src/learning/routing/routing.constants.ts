export const LEARNING_NODE_ROUTING_DEFAULTS = {
  basePath: '/',
  includeRoot: true,
  includeAcademyRoot: true,
  segmentAttributeKey: 'routeSegment'
} as const;

export const LEARNING_NODE_ROUTING_SEPARATOR = '/';
