{
  "name": "qubitel-academy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "generate:academy-manifests": "node scripts/generate-academy-manifest-imports.mjs",
    "preview": "vite preview",
    "lint": "npm run build",
    "test": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test",
    "test:unit": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test src/academies/*.test.js src/config/*.test.js src/config/*.test.ts src/learning/**/*.test.ts src/lib/*.test.js src/problems/*.test.js src/services/*.test.js src/utils/*.test.js src/utils/**/*.test.js",
    "test:integration": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test src/config/*.test.js src/config/*.test.ts src/services/*.test.js src/utils/*.test.js",
    "test:ci": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "compromise": "latest",
    "fuzzball": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@vitejs/plugin-legacy": "^8.0.2",
    "terser": "^5.48.0",
    "typescript": "latest"
  }
}
